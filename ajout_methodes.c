#include "etape3.h"



void ajout_methodes(char* path, int l, Methode x){
   /* int k;
    Configs* modif = config;   
    bool path_trouve = false;
    while (path_trouve != true){
        if (l == config->l){
            int k=0;
            while ( k<l && config->path[k]==path[k]){
                k++;
            }
            if (k==l){
                path_trouve=true;
            }
        }
        if ( modif->next != NULL && path_trouve!= true ){
            modif-> modif->next;
        }
        else return (-1); 
    }
    

    Methodes* meth= modif->c->m  
    while (meth->next!= NULL){
        if (meth->m == x){
            return (-1);  
        }
        meth= meth->next;
    }
    if (meth->m == x){
        return (-1);  
    }
    
    meth->next = malloc(sizeof(Langages)) ;
    meth -> m = x;*/
}
