#include "etape3.h"


int verifAccept(int q, Configs conf){
    /*Max=0;
    accepte = i_req-> c-> accept;
    while (accepte-> next != NULL){
        if (accepte-> a-> q < q){
            Max= max(accepte->a->q, Max);
        }
        accepte=accepte-> next;
    }
    
    if (accepte-> a-> q < q){
        Max= max(accepte->a->q, Max);
    }
    if (Max==0){
        return(-1);
    }
    accepte = i_req-> c-> accept;
    while (accepte-> next != NULL && accepte-> a-> q != Max){
        accepte=accepte-> next;
    }
    
    if (accepte-> a-> q != Max){
        accepte=accepte-> next;
    }
    int k=0;          
    while ( (k < accepte-> a-> l) && accepte-> a-> c[k]!="*"){
        k++;
    }
    
    if ( k== accepte-> a-> l){
        
    }
    
    if (k=1 && accepte->a->c[k+2]=="*"){
        recup
    }
*/
return 0;
}


