#include "etape3.h"

void ajout_langages(char* path, int l, Langage x){
    /*int k;
    Configs* modif = &config;   
    bool path_trouve = false;
    while (path_trouve != true){
        if (l == config.l){
            int k=0;
            while ( k<l && config.path[k]==path[k]){
                k++;
            }
            if (k==l){
                path_trouve=true;
            }
        }
        if ( modif.next != NULL && path_trouve!= true ){
            modif.modif->next;
        }
        else return (-1); 
    }
    

    Langages* lang= modif->c->l  
    while (lang->next!= NULL){
        if (lang->m == x){
            return (-2);  
        }
        lang= lang->next;
    }
    if (lang->m == x){
        return (-2);  
    }
    
    lang->next = malloc(sizeof(Langages)) ;
    lang -> m = x;
    return(0);*/
}
