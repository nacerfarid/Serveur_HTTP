#include "etape3.h"



void cas_double_etoile(){
    /*char* chaine = config-> path;
    char root= server_root;
    char* chemin;
    char* nom;
    int k=0, pos=0;
    for (k=0; k< config->l; k++){
        if (chaine[k]=='/'){
            pos = k;
        }
    }
    chaine[pos]='.';
    for (k=0; k< config->l; k++){
        if (chaine[k]=='/'){
            pos = k;
    
    for (k=0; k <= pos; k++){
        chemin[k]=chaine[k];
    }
    for (k=pos+1; k< config->l; k++){
        nom[k]=chaine[k];
    }
    concat_chaine(root, chemin);
    i_res->message = recup_fichier(root, pos+17, nom, (config->l)-pos);
*/
}



/* il faut calculer la taille !!!!! */
